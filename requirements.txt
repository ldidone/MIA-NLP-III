# CompuFix Agents — pinned, known-good dependency set.
#
# These bounds keep the whole LangChain 0.3 generation consistent. The newer
# langchain-chroma 1.x / langchain-huggingface 1.x require langchain-core >= 1.x,
# which conflicts with langgraph 0.2 and langchain 0.3 — hence the < upper bounds.
#
# IMPORTANT: this stack pins numpy < 2.0 (required by langchain 0.3 and
# langchain-chroma 0.1.x). numpy < 2.0 has no wheels for Python 3.13, so use
# Python 3.11 or 3.12 (see pyproject `requires-python`).

# Core multi-agent / LLM stack
langgraph>=0.2.28,<0.3
langchain>=0.3.0,<0.4
langchain-core>=0.3.15,<0.4
langchain-community>=0.3.0,<0.4
langchain-openai>=0.2.0,<0.3

# Vector store + embeddings
chromadb>=0.5.6,<0.6
langchain-chroma>=0.1.4,<0.2

# Pinned to satisfy the langchain 0.3 / langchain-chroma 0.1.x constraint.
numpy>=1.26.0,<2.0.0

# UI
streamlit>=1.36.0

# System inspection
psutil>=6.0.0

# Data models / config
pydantic>=2.7.0,<3.0.0
python-dotenv>=1.0.0

# Dev / quality / testing
pytest>=8.0.0
ruff>=0.5.0

# Optional: offline semantic retrieval with local embeddings.
# Not installed by default (pulls in torch). Enable via the [local] extra:
#   pip install -e ".[local]"
